
## Умолчания веб-морды — рецепт как дефолт  ← вскрыто E2E-прогоном 2026-08-13

Сквозной прогон «кот в Италии» упёрся ровно в несделанный пункт «Умолчания» шага 3
PROMPT-FORMAT-PLAN.md: форма веб-морды не умеет `--adaln-cache`, поэтому 8 шагов
отвергаются предохранителем (`schedule_not_baked`), и черновик стоит 48 минут вместо 13.

- [ ] Поле «Таблица AdaLN» в форме (или автоподстановка по числу шагов из `~/models/turbo/`)
- [ ] Дефолты формы = рабочий рецепт: чекпойнт `~/models/h3-8bit-full`, 8 шагов,
  LoRA `minimax_h3_turbo_v4_step600_ema.safetensors` сила 1.0, таблица `adaln_8_l100`
- [ ] То же для CLI: `h3 generate "текст"` без флагов должен давать рецепт, а не 4 бита × 31 шаг
