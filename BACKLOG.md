
## Умолчания веб-морды — рецепт как дефолт  ← вскрыто E2E-прогоном 2026-08-13

Сквозной прогон «кот в Италии» упёрся ровно в несделанный пункт «Умолчания» шага 3
PROMPT-FORMAT-PLAN.md: форма веб-морды не умеет `--adaln-cache`, поэтому 8 шагов
отвергаются предохранителем (`schedule_not_baked`), и черновик стоит 48 минут вместо 13.

- [ ] Поле «Таблица AdaLN» в форме (или автоподстановка по числу шагов из `~/models/turbo/`)
- [ ] Дефолты формы = рабочий рецепт: чекпойнт `~/models/h3-8bit-full`, 8 шагов,
  LoRA `minimax_h3_turbo_v4_step600_ema.safetensors` сила 1.0, таблица `adaln_8_l100`
- [ ] То же для CLI: `h3 generate "текст"` без флагов должен давать рецепт, а не 4 бита × 31 шаг

## Баг: dry-run маскирует битую таблицу чекпойнта  ← найден на упавшем коте 2026-08-13

`h3-8bit-full/transformer/adaln_cache.safetensors` был битым симлинком на неиспечённый
`adaln_8_plain.safetensors`. Дистилляция бага: `baked_grid_points()` при нечитаемой таблице
молча откатывается к константе 31 (`… or BAKED_GRID_POINTS`), dry-run пропускает задачу,
а реальный прогон падает через 20 с c `blocks.0.adaln_proj was not shipped` — после
полной загрузки 21 ГБ весов. Сорок минут не потеряли только случайно.

- [ ] Дать dry-run честный отказ: чекпойнт без своей таблицы и без `--adaln-cache` —
  это гарантированное падение, а не «требуется 31»
- [x] Таблица испечена 2026-08-13 (`adaln_8_plain.safetensors`, 204 МБ) — симлинк ожил,
  у `h3-8bit-full` родная сетка теперь 8 шагов

## Отложенные мелочи UX-волны 2026-08-14 (из леджера, не блокируют)

- [ ] Гейт паузы работника стоит после LLM-гейта — битый providers.json убивает воркер и на паузе
- [ ] is_paused кидает PermissionError вопреки докстрингу fail-open
- [ ] Кэш /media на год: при перезаписи файла тем же именем понадобится ?v=<mtime>
- [ ] На 360px длинный диалог скроллит подложку модалки, а не ленту (нужна отдельная раскладка)
- [ ] Нет socket-timeout у сервера (slow-loris) — предсуществующий паттерн, техдолг
- [ ] README: дописать /api/queue/pause|start, /api/llm/unload, /api/uploads, DELETE /api/chat
- [ ] _base_outdir срезает пользовательский каталог вида «штамп-слаг» (кладёт рядом, не внутрь)

## Чанкованный VAE decode/encode — портировать подход Kijai  ← новость 2026-08-12

Kijai смержил в core ComfyUI чанкованный encode/decode VAE MiniMax H3: куски стримятся в
выходной буфер, пик декода перестаёт зависеть от длины ролика. 175 кадров 1344×768:
decode VRAM 3,49→0,61 ГБ, encode 5,71→2,40 ГБ, RAM-пик декода +7,09→+2,32 ГБ, скорость та же,
выход побитово идентичен. Источник: https://t.me/cgevent/16350 →
https://www.reddit.com/r/StableDiffusion/s/5X5CKZMrii (PR в ComfyUI core, искать по «Kijai H3 VAE»).

Зачем нам: наш декод 1344×768 — 208 с и памятный пик, который вместе с 21 ГБ резидентного
8-битного DiT загнал сегодняшний kot-1344 в `metal::malloc 51.4 GB > 48 GB`. Чанкование по
времени с протаскиванием состояния каузальных свёрток через границы — переносимо в MLX;
«bitwise identical» у Kijai значит, что задача решаема точно, и есть референс для сверки
(как с таблицей AdaLN).

- [ ] Спайк: прочитать PR Kijai, оценить перенос чанкования в наш video_vae (MLX)
- [ ] Проверить и encode-сторону (i2v-кейфреймы длинных роликов)

## Face-refine проход — рецепт из ComfyUI-H3-FaceRefine  ← разведка 2026-08-14

Источник: https://github.com/Carasibana/ComfyUI-H3-FaceRefine. Вердикт: брать идеи, не код.
Лечит нашу намеренную слабость (лица при малой доле головы в кадре; 768×768 бьёт 512×512 втрое).
Рецепт: YOLO-детект → сглаженный трекинг кропа (раздельные окна центр/размер) → img2img
перегенерация кропа с denoise по размеру лица → вклейка face-box с feather+colour-match.
Против «кипения»: кроп-клип генерится целиком, не покадрово.

- [ ] Пререквизит: img2img-хук в нашем пайплайне — инъекция реального латента вместо нулей
  (аналог их H3 Inject Video Latent; у нас есть кейфрейм-механика — близко, но не то же)
- [ ] Минимальный вариант: YOLO(ultralytics, Mac ок) + savgol-сглаживание + rect-маска + affine paste
- [ ] Тонкости из README, которые не переизобрести: paste только face-box (не весь кроп),
  SAM-маска обычно ХУЖЕ прямоугольника (шов прячется в волосах), fade_out на кадрах без детекта
