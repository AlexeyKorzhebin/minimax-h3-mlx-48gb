# Бэклог minimax-h3-mlx-48gb

Единый список (слит из ~/models/BACKLOG.md 2026-08-17; сделанное вычеркнуто в git-истории обоих файлов).

## Очередь работ (согласована 2026-08-17)

### 1. Face-refine — ЗАПАРКОВАН 2026-08-18 (движок работает, доставка не даёт ценности)
Спека: docs/FEASIBILITY-face-refine.md (выполнимо, ~38–58 ч, блокеров нет).
Рецепт CV-обвязки: BACKLOG-история 2d38ba26 (YOLO → savgol → кроп → вклейка face-box).
**Эксперимент 2026-08-17: РАБОТАЕТ** (см. секцию «Эксперимент» в FEASIBILITY). Главный риск
(дистиллированное расписание на VAE-латентах реальных кадров) снят: sigma 0.30–0.50 дают
«то же лицо, больше деталей», оптимум 0.40 (деталь 1.40× при VAE-контроле 1.09×, дрейфа
и мерцания нет). 2 мин/проход на кропе 448×288×56к, пик 28.4 ГБ (энкодер текста, не диффузия).
Таблицы: ~/models/turbo/adaln_face_s0{30,40,50}_4pt_turbo.safetensors. Аудио-энкод исходника
не нужен (чистый шум даёт то же). Детектор лиц: cv2.CascadeClassifier удалён из OpenCV 5 —
нужен YuNet ONNX или ultralytics. Артефакты/скрипты: ~/Research/TestVideo/face-refine-exp/.
Итог: конвейер построен целиком (CLI `h3 face-refine`, d17ff736..d300b1e5, 1213 тестов),
боевые ворота: движок даёт ×1.46 на кропе, но вклейка с даунскейлом уничтожает прирост
на всех канвасах (0.62/0.67/1.00 при 448/896/1344). Подробно и условие разморозки
(апскейл-выдача, ~4–6 ч) — финальная секция docs/FEASIBILITY-face-refine.md.

### 2. Песни в mp3 — влилось в «Проекты» (волна 2026-08-18 идёт)
Тест «Колыбельной» (4 генерации + пробник) дал выводы, вшитые в волну «Проектов»:
- Формат: чистые структурные теги в лирике ([verse]/[chorus]...), ВСЯ режиссура — только
  в caption. Английские актёрские директивы в тегах лирики ЛОМАЮТ модель (переключение
  языка, несоответствие музыки голосу) — проверено пробником v3, не делать.
- Эмоциональная режиссура через caption работает частично (v2 лучше v1), но потолок
  8-битной Music3 по вокальной актёрке низкий: «нет печали, нет неизбежности». Вердикт
  пользователя: попса/фоновые треки — да, драматические произведения уровня suno — нет.
  Ограничение модели, дублями не лечится — больше GPU на дубли не жечь.
- Темп медленных песен ~28 с/секция; модель сама заканчивает раньше duration при
  исчерпанной лирике (это норма); недопетость ловится Whisper-проверкой.
- Мастеринг-цепочка (highpass 38 + EQ 7 точек + loudnorm −13.6) закрывает частотный
  разрыв с suno (низ/воздух), зафиксирована в h3_48gb/songrun.py.
Артефакты: ~/Research/TestVideo/music3-demo/kolybelnaya/ (4 версии + пробник + спектры).
Повторный разбор lyric-video-director (2026-08-18, по ссылке пользователя) — в волну взяты:
coverage-complete таймлайн (сцены покрывают 0→длительность трека, инструментальные гэпы
≥1.5 с — свои сцены, <1.5 с — вливаются), «песня священна» (без -shortest и time-stretch,
final == трек ±0.5 с, недостающее — freeze-frame), классификация гэпов по длительности.
Отложено на потом: tracer-пилот как формальный гейт (наша панель проекта и так показывает
первую сцену до конца счёта), караоке-типографика (не наш кейс — у нас видеоряд, не текст).
Волна реализована (Task 1-8, коммиты 396cf33e..ed137659), слияние и интеграционные ворота —
следующий шаг.

### 2-отложено. Проекты — мульти-клиповые сценарии (+ музыкальная дорожка) — РЕАЛИЗОВАНО волной 2026-08-18/19 (спека все три продукта)
Спека не написана. Суть: сценарий из N роликов, автокейфреймы из предпоследней секунды,
автосклейка, чат в контексте ролика сценария (как баллада, но кнопкой). Задел: kind:"clip"
в чат-сессиях. Идеи из lyric-video-director: coverage-complete timeline + валидатор,
visual bible, tracer-пилот, дисциплина ffmpeg-сборки.
**Эксперимент Music3 2026-08-17: УСПЕХ — проектировать сразу с дорожкой «песня».**
mlx-community/MiniMax-Music3-8bit + mlx-audio (pip, Python API: load_model(Path!) +
model.generate(text=caption, lyrics, duration, steps<=30, seed) → 44.1 кГц стерео).
Веса 14.18 ГБ в ~/models/music3/. Русский вокал разборчив (Whisper расшифровал почти
дословно), цензуры НЕТ (острые строки спеты дословно — связка «наш Qwen пишет → Music3
поёт» свободна из коробки). Стоимость: ~11× медленнее реалтайма (3 мин песни ≈ 33 мин),
пик до 32 ГБ → отдельный kind в очереди под тем же GPU-локом, что видео; загрузка 3.5 с
(mmap), можно резидентно. Грабли: load_model только Path (str виснет как HF repo id);
hf download на репо виснет — качать ~/models/music3/fetch-parallel.sh. Демо:
~/Research/TestVideo/music3-demo/ (night-studio-ru.mp3 90 с, obedience-test-ru.mp3 45 с,
спектрограммы, gen.py/analyze.sh в ~/models/music3/).

### 3. Кеширование шагов — ЗАКРЫТО 2026-08-18: не делать (разведка с замерами)
Кеш шагов и step-дистилляция — два способа купить одно и то же, и мы уже купили
(31→8 шагов, 3.7×). На нашем 7-форвардном расписании кешировать нечего, измерено на
наших весах одним сидом: ε между соседними форвардами 0.18–0.63 при порогах EasyCache
0.025–0.10 (пропускается НОЛЬ форвардов при любом опубликованном τ); посылка о
стабилизации k ложна (падает 45→1.1, в 40 раз); прогноз Spectrum хуже простого
копирования в 1.5–2×. Вынужденный пропуск по раскладке Spectrum впрыснул бы ≥19.7% пути
латента. У Bambushu работало на 20 шагах — к 7 не переносится (мейнтейнер kijai о
few-step: «кеш даёт то же, что просто меньше шагов»). Приз был бы большой (сэмплирование
91–96% wall), но настоящий рычаг времени — O(S²) внимания (см. «Дальше»/время декода).
Артефакты и A/B-стенд probe_ab_skip.py (на будущее): ~/Research/TestVideo/step-cache-recon/.
Что изменило бы ответ: возврат к недистиллированному расписанию (регресс, не путь).

## Дальше (не в очереди)

- [ ] Время декода: 10 с нативных = 11,6 мин VAE-компьюта; у чужого порта разрыв «вне
  сэмплирования» 2,56×. Память уже срезана (uint8-фикс), время не трогали.
- [x] CLI-умолчания: `h3 generate "текст"` без флагов должен давать рабочий рецепт
  (8bit-full, 8 шагов, LoRA, таблица) — СДЕЛАНО 70876cff: `--checkpoint`/`--steps`/
  `--turbo-lora`/`--adaln-cache` теперь по умолчанию воспроизводят ровно то, что шлёт
  веб-форма (DEFAULT_CHECKPOINT/DEFAULT_TURBO_LORA/DEFAULT_ADALN_CACHE/DEFAULT_STEPS в
  cli.py), 20 упавших test_cli.py (умолчание смотрело на h3-converted/transformer,
  снесённый чисткой диска 2026-08-17) почищены на tmp-фикстурах, не на реальных весах.
- [ ] NVFP4-кванты (`rockerBOO/minimax-h3-nvfp4`) — посмотреть резидент/качество.
- [ ] Эксперимент качества: сырой промпт vs развёрнутый диалоговым редактором — формальное
  сравнение одной сцены (было в плане H3-Context-IR, не закрыто).
- [ ] Скилл `writing-h3-video-prompts` по официальному гайду; перед написанием — baseline
  на субагентах (попадут ли в формат без гайда).
- [ ] Скилл downloading-hf-models: фаза REFACTOR (контрпримеры) + микро-тест формулировок.
- [ ] Вынести `.venv` из синхронизируемой Яндекс.Диском папки (однажды опустошался).
- [ ] `sudo sysctl iogpu.wired_limit_mb=45056` перед длинными прогонами (по умолчанию ~36 ГБ).
- [ ] Face-refine: тонкости из README не переизобретать (paste только face-box; SAM-маска
  обычно хуже прямоугольника; fade_out на кадрах без детекта).

## UX-мелочи — СДЕЛАНО 2026-08-17

Все 7 пунктов закрыты волной 04f51196..6dcc0c86 (слито в main 2d7faa45):
гейт паузы перед LLM-гейтом + fail-open is_paused (04f51196), _base_outdir спрашивает
записи очереди, а не форму имени (ac5e126f), socket-timeout 60 с (f5d80b9b),
?v=<mtime> для /media (2d426a15), README-маршруты (cd213fb0), 360px-скролл ленты
(6dcc0c86 — визуально в браузере не проверен, только логикой CSS; глянуть при случае
с телефона).
