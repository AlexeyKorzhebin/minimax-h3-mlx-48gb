# Рычаги памяти внимания — план

> REQUIRED SUB-SKILL: superpowers:subagent-driven-development / executing-plans.

**Goal:** Снизить пик форварда DiT на нативных длинных роликах с 44,8 до ~35 ГБ (15 с) четырьмя побитово-эквивалентными правками подготовки Q/K/V и MLP.

**Основа:** разведка 2026-08-17 (пробы `diffusion-peak-probe.py` / `diffusion-lever-probe.py` в скретче сессии — превратить в тесты). Все четыре варианта уже показали `max|Δ| = 0.0` против стока и −9,6 ГБ суммарно, время −2,5%.

## Global Constraints

- Чекаут: основной `/Users/aleksey.korzhebin/…/minimax-h3-mlx-48gb`, ветка `main` (upstream/ в .gitignore НЕ входит — проверь, правки в `upstream/minimax_h3_mlx/dit.py` идут обычным коммитом; если upstream гитигнорится — патч в `patches/` по образцу 0001).
- Побитовое парити — ворота каждой задачи: тест сравнивает выход блока/форварда сток vs правка на фиксированном сиде, allclose с atol=0 rtol=0. Плюс существующий `test_dit_parity`-класс тестов, если есть.
- Замер пика после каждого рычага (проба на 10 с форме, 2 блока) — цифру в коммит-месседж.
- Тесты `env -u NODE_OPTIONS .venv/bin/python -m pytest tests/ upstream/tests/ -q` перед каждым коммитом.
- GPU свободен только при неработающей очереди — проверь `/api/state` paused и пустоту running перед замерами.

## Task 1: rope-lean (−1,0…1,5 ГБ, ~1 ч)
`upstream/minimax_h3_mlx/dit.py:112` (apply_rotary): расширить cos/sin до полного head_dim единицами/нулями в непроворачиваемой зоне, убрать split/concat — 5 полноразмерных временных копий уходят. Парити-тест на блок.

## Task 2: qkv-split (−2…3 ГБ, ~3–4 ч)
Распил слитого `qkv_proj` на q/k/v при загрузке (гатеры по выходным строкам; scales квантованных весов идут по выходной строке — переставляются тем же индексом). Резидент не растёт (проверено разведкой). ДЕЛИКАТНО: `apply_backbone_lora` должен резать `lora_B` тем же индексом; `SplitQKVLoRALinear` упрощается. Парити-тест с LoRA и без.

## Task 3: q-chunk (−3,3 ГБ, ~2–3 ч)
SDPA чанками по query-строкам (k/v полные), чанк 8192. Парити-тест: чанкованный vs полный SDPA на случайном входе + полный блок.

## Task 4: mlp-chunk (−3,8 ГБ после Task 3, ~1 ч)
FeedForward чанками по строкам (тот же размер). Сам по себе нулевой — тест замера подтверждает выигрыш только в связке; парити-тест обязателен.

## Task 5: интеграционное закрепление
Полный форвард 50 блоков на 512×512 сток-против-всего: побитово; замер лестницы (проба) — таблицу в docs/RESULTS.md новой секцией «Attention memory levers» с цифрами до/после; краткое упоминание в docs/RUN-STATS.md шапке, что пики после 2026-08-17 ниже.
