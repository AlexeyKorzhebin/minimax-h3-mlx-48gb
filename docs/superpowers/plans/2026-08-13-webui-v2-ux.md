# Веб-морда v2 UX — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать 13 UX-замечаний ручной проверки + перевёрстку страницы по выбранному макету (школа Linear, тёмная+светлая темы на токенах).

**Architecture:** Этап A — функциональные правки поверх текущей вёрстки (API сессий/очереди/загрузок, слаги, стадии LLM). Этап C — перевёрстка `index.html`/`style.css`/рендеров `app.js` по канону `docs/superpowers/design-demos/variant-reference.html` (+`-light.html`): все цвета через CSS-токены, переключатель темы, три зоны. Логика `app.js` (analysePrompt, applyTurn, buildArgs и т.д.) не переписывается — меняются разметка, стили и рендер-функции.

**Tech Stack:** stdlib-only Python (`http.server`), ваниль JS/CSS, тесты pytest + node (`_node_eval`).

**Spec:** `docs/superpowers/specs/2026-08-13-webui-v2-ux-design.md`. **Дизайн-канон:** `docs/superpowers/design-demos/variant-reference.html` и `variant-reference-light.html` — реализация обязана брать оттуда палитры, типографику и структуру зон (файлы — образец, копировать код из них можно и нужно).

## Global Constraints

- Рабочая копия: `/Users/aleksey.korzhebin/Yandex.Disk.localized/Projects/h3-web-worktree` (ветка `h3-web`).
- Тесты: `.venv/bin/python -m pytest tests/<файл> -q`; перед финальным коммитом задачи — затронутые свиты целиком. Известный флак: mlx-тест, не чинить.
- Ни одной новой зависимости; только stdlib и ваниль. Никаких сборщиков.
- Тест сначала красный; мутационная проверка ключевой логики с текстом assert в отчёте.
- UI-тексты по-русски в тоне макета; комментарии в коде по-английски.
- Коммиты `feat:`/`fix:`/`test:` по-русски + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Секреты не попадают в git и ответы API. llama-server в тестах — только мок `tests/_fake_llama.py`.
- Существующие экспортируемые JS-функции и их тесты не ломать; рендер-функции менять можно, чистую логику — нельзя без причины.

---

## Этап A — функциональность

### Task A1: удаление чат-сессий и кнопка очистки

**Files:** Modify: `h3_48gb/web.py` (маршруты чата), `h3_48gb/webui/app.js`, `h3_48gb/webui/index.html`; Test: `tests/test_chat_web.py`, `tests/test_web.py`.

**Interfaces:**
- Produces: `DELETE /api/chat/<id>` → удаляет `<outdir>/chat/<id>.json` и `<id>.lock` (если есть), `{"ok": true}`; 404 `chat_not_found` на неизвестный; id через существующий `resolve_within`-паттерн (`_chat_path`).
- Produces (JS): кнопка «очистить» в модалке — confirm → DELETE → модалка переходит в пустую новую сессию (`openChatModal({kind:"new"})`).

- [ ] Красные тесты: DELETE удаляет оба файла; DELETE несуществующей → 404; DELETE во время хода (flock занят) → 409 `chat_busy` (лок проверяется тем же `chat_session_lock`, не блокируясь).
- [ ] Реализация по образцу `_cancel_job`/`_read_chat`. Кнопка в `chat-head` рядом с «закрыть».
- [ ] Зелёный, мутация (убери удаление `.lock` → тест красный), коммит `feat: удаление чат-сессии вместе с её замком`.

### Task A2: стадии LLM в модалке + очистка поля ввода при отправке

**Files:** Modify: `h3_48gb/web.py` (`_chat_message`), `h3_48gb/webui/app.js`, `style.css`; Test: `tests/test_web.py` (JS), `tests/test_chat_web.py`.

**Interfaces:**
- Produces: до вызова провайдера сервер ничего не стримит (архитектура синхронная) — стадии показывает клиент: сразу после клика в ленте появляется анимированная запись-плейсхолдер («поднимаю модель…» если `/api/llm` даёт down, иначе «модель думает…») с CSS-анимацией точек; `landTurn`/`landFailure` заменяют её ответом. Поле ввода очищается сразу при отправке; при ошибке текст возвращается в поле (существующее поведение сохранить).
- Produces (JS): `export function pendingEntry(llmStatus)` → `{kind:"pending", text:"поднимаю модель…"|"модель думает…"}` — чистая, тестируемая.

- [ ] Красные JS-тесты: `pendingEntry("down").text` содержит «поднимаю», `pendingEntry("up")` — «думает»; после `landTurn` pending-записи в логе нет (расширь тест applyTurn/landTurn: state.log с pending-записью → после landTurn она удалена).
- [ ] Реализация: `sendChatMessage` пушит pending-запись и чистит `#chat-input` сразу; `landTurn`/`landFailure` первым делом выкидывают pending из `log`; при guard-выходе (чужая сессия) pending тоже вычищается по ссылке на state.
- [ ] Зелёный, мутация (pending не удаляется → тест красный), живая проверка в браузере с delay-моком (опиши в отчёте), коммит.

### Task A3: длительность в модалке → контекст LLM и разбор

**Files:** Modify: `h3_48gb/web.py` (`_create_chat`, `_chat_message`, `_locked_turn`), `h3_48gb/webui/app.js`, `index.html`; Test: `tests/test_chat_web.py`, `tests/test_web.py`.

**Interfaces:**
- Produces: сессия хранит `duration` (число секунд; при создании из формы — значение `#duration`, из задачи — из args `--duration`, дефолт 10); поле «Длительность, с» в шапке модалки, редактируемое; уходит в тело хода (`duration` в allowlist) и в system-контекст строкой `duration: N s`; `paintPrompt` модалки использует его вместо значения формы.

- [ ] Красные тесты: python — system содержит `duration: 7 s` при сессии с duration 7; JS — разбор в модалке получает длительность сессии (вынеси выбор длительности в чистую `chatDuration(state)` и покрой).
- [ ] Реализация, зелёный, мутация (duration из формы вместо сессии → тест красный), коммит.

### Task A4: слаг от LLM + эвристика; тег и имена из слага

**Files:** Modify: `h3_48gb/provider.py` (`PROMPT_SCHEMA` + системный промпт-док `docs/h3-prompt-system.md`), `h3_48gb/web.py`, `h3_48gb/webui/app.js`; Test: `tests/test_provider.py`, `tests/test_chat_web.py`, `tests/test_web.py`.

**Interfaces:**
- Produces: `PROMPT_SCHEMA` получает необязательное поле `slug` (`type: ["string","null"]`, в `properties` объекта верхнего уровня, НЕ в required — старые ответы валидны); `docs/h3-prompt-system.md` — абзац: «когда возвращаешь prompt, добавь slug: 2–4 латинских слова через дефис, суть сцены (пример: cat-italian-noon)».
- Produces: сессия хранит последний непустой `slug`; «в Редактор» подставляет его в `#tag`.
- Produces (JS): `export function heuristicSlug(promptText)` — транслит первых 3 значимых слов `integrated_multimodal_description` (пропуская `[Shot 1]`, стиль-слова `Live-action, cinematic`), максимум 24 символа, `[a-z0-9-]`; используется как тег при постановке из формы, если `#tag` пуст или равен `run`.

- [ ] Красные тесты: схема содержит slug и он необязателен (валидный ответ без slug проходит `chat`); ход с ответом, содержащим slug, сохраняет его в сессию и отдаёт клиенту; JS — `heuristicSlug("integrated_multimodal_description: [Shot 1] Live-action, cinematic, a wide shot captures a sun-drenched Italian cobblestone street…")` → например `wide-shot-captures` или лучше (первые содержательные слова; зафиксируй поведение тестом), кириллица транслитерируется.
- [ ] Реализация, зелёный, мутации (slug теряется при сохранении сессии → красный; эвристика возвращает пусто → красный), коммит.

### Task A5: пауза/старт очереди

**Files:** Modify: `h3_48gb/queue.py` (маркер), `h3_48gb/worker.py` (`main_loop`), `h3_48gb/web.py` (маршруты + `/api/state`), `h3_48gb/webui/app.js`, `index.html`; Test: `tests/test_queue.py`, `tests/test_worker.py`, `tests/test_chat_web.py`, `tests/test_web.py`.

**Interfaces:**
- Produces: `queue.is_paused(root) -> bool` / `queue.set_paused(root, value: bool)` — маркер-файл `<queue_root>/paused` (существование = пауза), durably не нужен (потеря маркера = очередь работает, безопасное направление, задокументируй в docstring).
- Produces: `main_loop` после reconcile и LLM-гейта: `if q.is_paused(root): sleep(poll); continue` — текущая задача не прерывается (проверка до claim).
- Produces: `POST /api/queue/pause`, `POST /api/queue/start` → `{"ok": true, "paused": bool}`; `/api/state` дополняется `"paused": bool`. **Свежая очередь стартует НА ПАУЗЕ**: `queue.layout(root)` при создании каталога очереди создаёт и маркер (только при создании, не при каждом вызове — иначе перезапуск воркера ставил бы паузу).
- Produces (JS): кнопка состояния очереди «▶ Начать расчёт»/«⏸ Приостановить» по `state.paused`; `unloadBanner`/`nextBannerState` показывают плашку выгрузки только при `paused: false` (расширь их вход полем paused и тесты).

- [ ] Красные тесты: worker с pending-задачей и маркером не берёт её, снятие маркера — берёт (по образцу интеграционного теста LLM-гейта); свежий layout создаёт маркер, повторный layout существующей очереди — не создаёт; маршруты переключают; плашка не показывается на паузе.
- [ ] Реализация, зелёный, мутация (гейт паузы закомментирован → тест красный), коммит.

### Task A6: папка на задачу

**Files:** Modify: `h3_48gb/queue.py` (`submit` — вычисление outdir задачи), `h3_48gb/cli.py` (если сборка путей вывода там), `h3_48gb/webui/app.js` (ссылки результатов); Test: `tests/test_queue.py`, `tests/test_chat_web.py`.

**Interfaces:**
- Produces: результат задачи ложится в `<outdir>/<YYYYMMDD-HHMM>-<слаг>/` (слаг = тег задачи после `_slug`-очистки): `submit` переписывает/дополняет `--outdir` в args задачи на подкаталог. Изучи на месте, как args несут `--outdir` и как воркер их исполняет — подкаталог должен создать сам `h3 generate` (проверь, создаёт ли он outdir; если нет — создавай в `run_job` перед spawn). Старые задачи и прямые CLI-запуски не трогаются (правило только для `submit`).
- Produces (JS): строка результата ссылается на `/media/<подкаталог>/<файл>` — проверь, как `_media` отдаёт вложенные пути (resolve_within уже есть).

- [ ] Красные тесты: `submit` с тегом `kot-italy` кладёт в args `--outdir <outdir>/<штамп>-kot-italy`; воркер исполняет задачу и файлы оказываются в подкаталоге (интеграционный, с фейковым spawn, создающим файл по args); `/media` отдаёт файл из подкаталога.
- [ ] Реализация, зелёный, мутация (подкаталог без слага → красный), коммит.

### Task A7: загрузка файлов — POST /api/uploads + drag&drop поля

**Files:** Modify: `h3_48gb/web.py`, `h3_48gb/webui/app.js`, `index.html`, `style.css`; Test: `tests/test_chat_web.py`, `tests/test_web.py`.

**Interfaces:**
- Produces: `POST /api/uploads` — тело: сырые байты, заголовки `Content-Type: application/octet-stream`, `X-Filename: <имя>` (имя санитизируется: basename, безопасные символы, длина ≤100); файл → `<outdir>/uploads/<штамп>-<имя>`; ответ `{"ok": true, "path": "<абсолютный путь>"}`. Ограничения: суффиксы `CHAT_IMAGE_SUFFIXES` (это загрузка кадров), размер ≤ `CHAT_IMAGE_MAX_BYTES` → 400 `bad_image`; `MAX_BODY_BYTES` существующий — проверь, что не мешает 16 МБ (подними для этого маршрута осознанно, задокументируй).
- Produces (JS): поля «Первый кадр»/«Последний кадр» становятся зоной: drag&drop файла ИЛИ клик → `<input type=file hidden>`; после загрузки в зоне имя файла + миниатюра (`/media`-путь не нужен — просто имя), путь в скрытом поле уходит в args как раньше; ручной ввод пути — по ссылке «указать путь» (fallback).

- [ ] Красные тесты: python — upload кладёт файл и возвращает путь внутри outdir; не-картинка → 400; превышение размера → 400; traversal в X-Filename (`../x.png`) не выходит из uploads/. JS — чистая `uploadZoneLabel(state)` (пусто → «перетащи…», загружено → имя файла) покрыта node-тестом.
- [ ] Реализация, зелёный, мутация (санитизация имени убрана → traversal-тест красный), живая проверка dnd в браузере (отчёт), коммит.

### Task A8: картинка в диалог (скрепка + dnd в чат)

**Files:** Modify: `h3_48gb/web.py` (`_chat_message` — приём `image` в теле хода), `h3_48gb/webui/app.js`, `index.html`, `docs/h3-prompt-system.md`; Test: `tests/test_chat_web.py`.

**Interfaces:**
- Consumes: `POST /api/uploads` (A7).
- Produces: поле ввода чата принимает dnd/скрепку → файл уходит через `/api/uploads`, путь сохраняется в сессию (`image`, и `mode` сессии становится `i2v`, если был t2va/пуст) тем же ходом: тело `message` получает необязательные `image`/`set_mode`; сервер валидирует как при создании (`_chat_image_path`); кадр прикладывается к ходу (существующий механизм), «в Редактор» подставляет режим i2v и путь кадра в форму.
- Produces: абзац в `docs/h3-prompt-system.md`: «если пользователь приложил картинку без слов — опиши её и предложи промпт от неё».

- [ ] Красные тесты: ход с `image` обновляет сессию (image+mode) и шлёт модель image_url-часть; невалидный файл → warning как раньше; «в Редактор»-путь покрывается расширением существующего JS-теста finishChat (mode/image уходят в форму).
- [ ] Реализация, зелёный, мутация (mode не переключается → красный), коммит.

---

## Этап C — перевёрстка по канону Linear-школы

### Task C1: токены и две темы

**Files:** Modify: `h3_48gb/webui/style.css` (полная замена палитры на токены), `index.html` (переключатель), `app.js` (хранение выбора); Test: `tests/test_web.py`.

**Interfaces:**
- Produces: все цвета `style.css` — через custom properties, значения обеих палитр берутся ИЗ МАКЕТОВ `docs/superpowers/design-demos/variant-reference.html` (тёмная) и `variant-reference-light.html` (светлая, в его шапке пары токенов). Тёмная — на `:root`; светлая — `@media (prefers-color-scheme: light)` с guard `:root:not([data-theme=dark])` + явные `[data-theme=light]`/`[data-theme=dark]`. Переключатель в шапке (три состояния: системная/тёмная/светлая), выбор в localStorage.
- Produces (JS): `export function nextTheme(current)` цикл system→dark→light→system + `applyTheme(value)` (data-атрибут на `<html>`).

- [ ] Красные JS-тесты: `nextTheme` цикл; в `style.css` нет захардкоженных hex вне блока определения токенов (структурный тест: grep-логика по файлу — hex допустимы только в первых N строках/секции токенов; реализуй проверку так, чтобы она не была хрупкой — например, маркеры-комментарии `/* tokens */ … /* /tokens */`).
- [ ] Реализация, зелёный, скриншоты обеих тем в отчёт, коммит.

### Task C2: раскладка страницы — три зоны по макету

**Files:** Modify: `h3_48gb/webui/index.html`, `style.css`, `app.js` (рендеры очереди/результатов); Test: `tests/test_web.py`.

**Interfaces:**
- Produces: структура по `variant-reference.html`: шапка-телеметрия (адрес, VRAM/LLM-статус, паузa/расчёт); зона 1 «Сочинение» (редактор с существующими hl/scale/parse, кнопки «Новый через диалог»/библиотека, параметры), зона 2 «Очередь» (крупное состояние + кнопка старта из A5, текущая задача с сегментами проходов, ждущие, плашка выгрузки), зона 3 «Готово» (карточки с превью — существующий preview jpg задачи как `<img>`, слаг, время счёта, действия). Сетки/отступы/типографика — из макета. Существующие id, на которые завязан `app.js`, сохранить или аккуратно переименовать со сквозной правкой.
- Produces: выпадашка разрешений вместо кнопок: черновик/малое/большое × гориз./верт. + «своё…» (показывает поля ширина/высота); `CANVAS_PRESETS` расширяется вертикальными, `applyCanvasPreset` остаётся (тесты обнови: 6 пресетов + custom); «Настройки модели» в `<details>` со сводкой значений в свёрнутом виде (как в макете); поле «Заметка» удалить из формы (перед удалением проверь: если note где-то показывается в очереди — оставь колонку, убери только поле ввода; задокументируй решение в отчёте).

- [ ] Красные тесты: CANVAS_PRESETS = 6 + вертикальные размеры корректны (288×448 и т.д.); JS-тест сводки `<details>` (чистая функция `modelSummary(form)` → «h3-8bit · 8 шагов · LoRA 1.0 · таблица l100»).
- [ ] Реализация (можно и нужно переносить куски разметки/CSS из макета), прогон всей tests/test_web.py — существующие DOM-тесты чинить только если они проверяли удалённое/переименованное, не ослабляя.
- [ ] Живая проверка: обе темы, ширины 1440/1024/700, скриншоты в отчёт. Коммит.

### Task C3: модалка по макету + адаптивность страницы

**Files:** Modify: `index.html`, `style.css`, `app.js`; Test: `tests/test_web.py`.

**Interfaces:**
- Produces: модалка в стилистике макета (второй экран `variant-reference.html`): окно промпта + лента, шапка с провайдером/длительностью (A3)/статусом/кнопками (включая «очистить» из A1), пустое состояние с подсказкой; стадии LLM из A2 стилизуются по макету. Страница целиком держит 360–1920px: сетки minmax/auto-fit, таблица очереди скроллится внутри, ничего не вылезает за край (то самое замечание про кнопки).

- [ ] Живая проверка: 360/700/1024/1440/1920 в обеих темах, скриншоты; клик-тест полного пути (диалог-мок → редактор → постановка) через браузер с fake-провайдером.
- [ ] Прогон всех JS/веб-свитов, коммит.

### Task C4: сквозной смоук и хвосты

- [ ] Полный `.venv/bin/python -m pytest tests/ -q` — итог в отчёт.
- [ ] Живой смоук на временном outdir: страница открывается, очередь на паузе по умолчанию, постановка задачи с фейковым spawn — файлы в подкаталоге со слагом, «начать расчёт» снимает паузу.
- [ ] Обновить `docs/superpowers/specs/2026-08-13-webui-v2-ux-design.md` разделом «Реализовано» со списком коммитов. Коммит хвостов.
