# Runtime dependencies for this fork (h3_48gb/) and the vendored upstream port it patches.
#
# upstream/requirements.txt lists only mlx, numpy, safetensors and huggingface_hub — that omits
# mlx-vlm, which upstream/minimax_h3_mlx/text_encoder.py imports unconditionally (h3_48gb wraps
# that same module), and pillow, which h3_48gb/preview.py uses to write preview JPEGs. Installing
# only upstream's list makes `h3 generate` fail at import time with
# `ModuleNotFoundError: No module named 'mlx_vlm'`. Install from this file instead — see README.md
# step 0.

mlx>=0.32.0
mlx-vlm>=0.6.10
numpy>=1.26
safetensors>=0.4
huggingface_hub>=0.30
pillow>=10.0

# Test suite: ./.venv/bin/python -m pytest tests/ test_preview.py -q
pytest>=8.0
